

    <%= form_with(@a.requests_sent.new, url: user_friendships_path, local: true) do |f| %>
  <%= f.hidden_field :active, value: current_user.id %>
  <%= f.hidden_field :passive, value: @user.id %>
  <button type="submit" class="btn btn-primary">send request</button>
    <% end %>