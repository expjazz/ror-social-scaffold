<section class="user-section">
  <%= render_search(@user) %>
  <%= render_username(@user)%>
  <article class="timeline">
    <h3>Recent posts:</h3>
    <ul class="posts">
      <%= render @posts %>
    </ul>
  </article>
</section>
